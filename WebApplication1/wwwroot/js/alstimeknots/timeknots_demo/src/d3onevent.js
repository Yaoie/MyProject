// var circles = document.querySelectorAll(`${id} > svg > g > circle`);
            // var txts = document.querySelectorAll(`${id} > svg > g > text`);

            // d3.selectAll(`${id} > svg > g`)
            //     .data(events)
            //     .on("mouseover", (d, i) => {

            //         var circle = circles[i];
            //         var txt = txts[i];

            //         knotOn(d, cfg, circle, txt);
            //         if (cfg.showTooltip) {
            //             labelOn(d, cfg, tip);
            //         }
            //         console.log("mouse over d: " + JSON.stringify(d));
            //         console.log("mouse over i: " + JSON.stringify(i));
            //         console.log("mouse over d3 event: " + JSON.stringify(d3.event));
            //     })
            //     .on("mouseoout", (d, i) => {

            //         var circle = circles[i];
            //         var txt = txts[i];

            //         knotOff(d, cfg, circle, txt);
            //         if (cfg.showTooltip) {
            //             labelOff(tip);
            //         }
            //     });